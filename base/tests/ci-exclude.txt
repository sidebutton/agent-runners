# base/tests/ci-exclude.txt — tests run-all.sh SKIPS by default (and in CI).
#
# These guards are correct, but NOT hermetic on a generic / CI runner: they pass on a
# truly clean runner but FALSE-FAIL when the host carries provisioned state. Listed
# here with the reason so the exclusion is reviewable, not silent. `run-all.sh --all`
# runs them anyway (e.g. on a clean throwaway box). Re-home each to T8 (SCRUM-1451,
# end-to-end + CI hardening) once it is made host-independent, then delete it here.
#
# Format: one test basename per line. Lines starting with # are comments.
#
# test-sb-self-update.sh — SCRUM-1380 guard. Its "stranded bin link => relink (self-heal)"
#   case (4d-E) verifies CLI health with `command -v sidebutton && sidebutton --version`
#   (base/lib-refresh.sh:145). On any host that ALSO has a real global `sidebutton` on
#   PATH — i.e. every provisioned agent VM — the stub-stranded sandbox link still
#   resolves the REAL binary, the install looks healthy, the sandbox relink is skipped,
#   and the case false-fails. It is green on a clean runner with no global sidebutton.
#   Out of T4 scope (component model + CCR); fix = make that health check prefix-local.
test-sb-self-update.sh
